const {chromium}=require('playwright');
const fs=require('node:fs');
const base=process.env.BASE_URL||'http://127.0.0.1:8799',out='docs/phase35d/screenshots-staging';fs.mkdirSync(out,{recursive:true});
(async()=>{const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});for(const width of [390,1440]){const page=await browser.newPage({viewport:{width,height:900}});await page.goto(base+'/learn.html?lang=en&stage=1');await page.locator('.lesson-card').first().waitFor();await page.screenshot({path:`${out}/${width}-lesson-list.png`,fullPage:true});await page.goto(base+'/lesson.html?id=ja-s3-l1&lang=ja&stage=3');await page.locator('.lesson-hero').waitFor();await page.locator('[data-step="grammar"]').click();await page.screenshot({path:`${out}/${width}-lesson-detail.png`,fullPage:true});await page.close();}await browser.close();console.log('Phase 3.5D staging visual captures complete');})().catch(e=>{console.error(e);process.exit(1);});
