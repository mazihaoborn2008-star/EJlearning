import fs from 'node:fs';
import {spawnSync} from 'node:child_process';
const persist=process.env.PHASE35D_PERSIST||'.wrangler/phase35d-clean';
function query(sql){const r=spawnSync(process.execPath,['node_modules/wrangler/bin/wrangler.js','d1','execute','DB','--local','--config','wrangler.35d.local.jsonc','--persist-to',persist,'--command',sql,'--json'],{encoding:'utf8',maxBuffer:20*1024*1024});if(r.status)throw Error(r.stderr||r.stdout);return JSON.parse(r.stdout.slice(r.stdout.indexOf('[')))[0].results;}
const q=v=>v===null?'NULL':typeof v==='number'?String(v):`'${String(v).replaceAll("'","''")}'`;
const insert=(table,columns,data)=>data.length?`INSERT INTO ${table}(${columns.join(',')}) VALUES\n${data.map(r=>`(${columns.map(c=>q(r[c])).join(',')})`).join(',\n')};\n`:'';
const units=query('SELECT id,language,stage,topic_id,title,objective,sequence,status,estimated_minutes FROM lesson_units ORDER BY language,stage,sequence');
const prerequisites=query('SELECT lesson_id,prerequisite_lesson_id FROM lesson_prerequisites ORDER BY lesson_id');
const items=query('SELECT lesson_id,content_type,content_id,role,sequence,required FROM lesson_items ORDER BY lesson_id,content_type,sequence');
const exams=query('SELECT framework_id,target,lesson_id,relevance FROM lesson_exam_recommendations ORDER BY framework_id,target,lesson_id');
const sql=`-- Phase 3.5D composition-only D1. Canonical content remains in the read-only CONTENT_DB binding.\nPRAGMA foreign_keys=ON;\nCREATE TABLE lesson_units (id TEXT PRIMARY KEY,language TEXT NOT NULL CHECK(language IN ('en','ja')),stage INTEGER NOT NULL CHECK(stage BETWEEN 1 AND 6),topic_id TEXT NOT NULL,title TEXT NOT NULL,objective TEXT NOT NULL,sequence INTEGER NOT NULL,status TEXT NOT NULL CHECK(status IN ('draft','published','archived')),estimated_minutes INTEGER NOT NULL CHECK(estimated_minutes BETWEEN 10 AND 20),UNIQUE(language,stage,sequence));\nCREATE INDEX lesson_path_browse ON lesson_units(language,status,stage,sequence);\nCREATE TABLE lesson_prerequisites (lesson_id TEXT NOT NULL REFERENCES lesson_units(id),prerequisite_lesson_id TEXT NOT NULL REFERENCES lesson_units(id),PRIMARY KEY(lesson_id,prerequisite_lesson_id),CHECK(lesson_id<>prerequisite_lesson_id));\nCREATE TABLE lesson_items (lesson_id TEXT NOT NULL REFERENCES lesson_units(id),content_type TEXT NOT NULL CHECK(content_type IN ('vocabulary','grammar','expression')),content_id TEXT NOT NULL,role TEXT NOT NULL CHECK(role IN ('required','support','exposure')),sequence INTEGER NOT NULL,required INTEGER NOT NULL CHECK(required IN (0,1)),PRIMARY KEY(lesson_id,content_type,content_id),CHECK((role='required' AND required=1) OR (role<>'required' AND required=0)));\nCREATE INDEX lesson_items_resolve ON lesson_items(lesson_id,content_type,sequence);\nCREATE INDEX lesson_items_coverage ON lesson_items(content_type,content_id);\nCREATE TABLE lesson_exam_recommendations (framework_id TEXT NOT NULL,target TEXT NOT NULL,lesson_id TEXT NOT NULL REFERENCES lesson_units(id),relevance INTEGER NOT NULL CHECK(relevance BETWEEN 1 AND 3),PRIMARY KEY(framework_id,target,lesson_id));\nCREATE INDEX lesson_exam_lookup ON lesson_exam_recommendations(framework_id,target,relevance DESC,lesson_id);\n${insert('lesson_units',['id','language','stage','topic_id','title','objective','sequence','status','estimated_minutes'],units)}${insert('lesson_prerequisites',['lesson_id','prerequisite_lesson_id'],prerequisites)}${insert('lesson_items',['lesson_id','content_type','content_id','role','sequence','required'],items)}${insert('lesson_exam_recommendations',['framework_id','target','lesson_id','relevance'],exams)}`;
fs.mkdirSync('migrations-35d-layer',{recursive:true});fs.writeFileSync('migrations-35d-layer/0001_lessons.sql',sql);console.log(`Generated composition layer: ${units.length} lessons, ${items.length} links, ${exams.length} exam recommendations.`);
